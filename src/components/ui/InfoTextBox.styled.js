import styled from 'styled-components';

const InfoTextBox = styled.section`
	position: relative;
	overflow: hidden;
	width: 100%;
	height: 20vh;
	padding: 4rem;
	margin: 4rem 0;
	background-image: url('/contourLine.svg');
`;

export default InfoTextBox;
