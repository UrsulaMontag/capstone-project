import styled from 'styled-components';

const HeadingBox = styled.header`
	background: var(--detail-color);
	text-align: center;
	width: 100vw;
	padding: 1.2rem;
	position: fixed;
	top: 0;
	overflow: hidden;
	z-index: 1000;
`;

export { HeadingBox };
